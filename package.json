{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.18.1"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "deploy:app": "now --local-config app.now.json",
    "deploy:website": "now --local-config website.now.json",
    "deploy:all": "yarn deploy:app & yarn deploy:website"
  }
}
